<div class="container-fluid d-flex justify-content-center align-items-center flex-column">

<h1>URL Shortener</h1>
<img src="/images/logo.png" alt="" height="200" width="200">
<form action="/urls" method="post">
  <input type="url" name="longurl" placeholder="Enter your URL here">
  <br>
  <button class="btn btn-outline-dark btn-block" type="submit">Shorten</button>
</form>
{{#if invalidurl}}
<p class="alert alert-danger">You entered a wrong URL. Please try again.</p>
{{/if}}
</div>
